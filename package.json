{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "cra-template": "1.2.0",
    "react": "^17.0.0",          // Downgrade React to a stable version compatible with react-scripts 5.0.1
    "react-dom": "^17.0.0",      // Downgrade ReactDOM to match React version
    "react-router-dom": "^5.2.0", // Downgrade react-router-dom to a stable version compatible with React 17
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11",
    "web-vitals": "^4.2.4"
  },
  "engines": {
    "node": "16.20.2" // Use the version supported by Netlify
  }
}
